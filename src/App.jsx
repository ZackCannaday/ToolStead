import { useEffect, useMemo, useRef, useState } from "react";
import {
  AddressBook, ArrowRight, Bell, CalendarBlank, CaretDown, ChartLineUp,
  ChatCircleDots, Check, CheckCircle, ClipboardText, CurrencyDollar,
  EnvelopeSimple, Funnel, GlobeHemisphereWest, GridFour, ImageSquare,
  MagnifyingGlass, NotePencil, Package, PaperPlaneTilt, PhoneCall, Plus,
  ShieldCheck, Sparkle, SpinnerGap, Storefront, Tray, Users, Wrench, X,
} from "@phosphor-icons/react";
import {
  ApiError, addContactNote, archiveContact, checkApi, createContact,
  getContacts, getModules, login, updateContact,
} from "./api.js";

// # Preview records
const PREVIEW_CONTACTS = [
  { id: "preview-jamie", displayName: "Jamie Patel", companyName: "", email: "jamie.patel@example.com", phone: "(706) 555-0132", source: "Website form", stage: "New lead", updatedAt: "2026-09-01T12:45:00.000Z", timeline: [
    { id: "event-1", eventType: "lead_created", title: "Website lead received", text: "Requested a full-detail quote and weekday availability.", occurredAt: "2026-09-01T12:45:00.000Z" },
    { id: "event-2", eventType: "page_viewed", title: "Pricing page viewed", text: "Full Detail Package", occurredAt: "2026-09-01T12:41:00.000Z" },
  ] },
  { id: "preview-luis", displayName: "Luis Williams", companyName: "Williams Property Care", email: "luis.williams@example.com", phone: "(706) 555-0148", source: "Missed call", stage: "Contacted", updatedAt: "2026-09-01T11:10:00.000Z", timeline: [
    { id: "event-3", eventType: "note_added", title: "Follow-up needed", text: "Asked for an estimate by email.", occurredAt: "2026-09-01T11:10:00.000Z" },
  ] },
  { id: "preview-sarah", displayName: "Sarah Green", companyName: "", email: "sarah.green@example.com", phone: "(706) 555-0191", source: "Booking page", stage: "Qualified", updatedAt: "2026-08-31T18:15:00.000Z", timeline: [
    { id: "event-4", eventType: "appointment_created", title: "Appointment requested", text: "Interior detailing · Wednesday at 2:00 PM", occurredAt: "2026-08-31T18:15:00.000Z" },
  ] },
  { id: "preview-maria", displayName: "Maria Chen", companyName: "", email: "maria.chen@example.com", phone: "(706) 555-0177", source: "Manual entry", stage: "Won", updatedAt: "2026-08-30T21:20:00.000Z", timeline: [
    { id: "event-5", eventType: "message_sent", title: "Estimate accepted", text: "Brake service estimate #12651", occurredAt: "2026-08-30T21:20:00.000Z" },
  ] },
];

// # Tool registry
const TOOL_REGISTRY = [
  { key: "crm-core", name: "Lead Intake & CRM", description: "Capture every lead, keep contact details together, and follow the complete activity timeline.", category: "Sales", status: "active", icon: AddressBook, accent: "blue", included: "Core platform" },
  { key: "site-builder", name: "Sites, Funnels & Forms", description: "Build websites, landing pages, surveys, forms, and conversion funnels.", category: "Marketing", status: "available", icon: GlobeHemisphereWest, accent: "violet", included: "Marketing bundle" },
  { key: "booking", name: "Booking & Calendar", description: "Publish availability, collect appointment requests, and prevent schedule conflicts.", category: "Operations", status: "available", icon: CalendarBlank, accent: "teal", included: "Growth bundle" },
  { key: "messaging", name: "Messaging Hub", description: "Manage consent-based email, SMS, voice, and missed-call follow-up in one inbox.", category: "Communication", status: "available", icon: ChatCircleDots, accent: "orange", included: "Growth bundle" },
  { key: "smart-intake", name: "MarginPilot Quotes", description: "Price work accurately with labor, materials, overhead, tax, and target margin.", category: "Sales", status: "available", icon: ClipboardText, accent: "indigo", included: "Sales add-on" },
  { key: "payments", name: "Payments & Invoices", description: "Send payment links, track balances, and automate polite overdue follow-up.", category: "Finance", status: "planned", icon: CurrencyDollar, accent: "green", included: "Finance add-on" },
  { key: "media-kit", name: "Before & After Media Kit", description: "Brand, compress, organize, and prepare customer-ready service photos.", category: "Marketing", status: "planned", icon: ImageSquare, accent: "pink", included: "Marketing add-on" },
  { key: "analytics", name: "Analytics & Attribution", description: "Connect leads, appointments, payments, and revenue back to their source.", category: "Reporting", status: "planned", icon: ChartLineUp, accent: "slate", included: "Growth bundle" },
];

const NAV_ITEMS = [
  { id: "my-tools", label: "My Tools", icon: GridFour },
  { id: "crm", label: "Leads & CRM", icon: AddressBook, toolKey: "crm-core" },
  { id: "library", label: "Tool Library", icon: Package },
];
const EMPTY_LEAD = { displayName: "", companyName: "", email: "", phone: "", source: "Manual entry", summary: "" };

function initials(name = "") { return name.split(/\s+/).filter(Boolean).slice(0, 2).map((part) => part[0]?.toUpperCase()).join("") || "?"; }
function formatDate(value) { return value ? new Intl.DateTimeFormat("en-US", { month: "short", day: "numeric", hour: "numeric", minute: "2-digit" }).format(new Date(value)) : "Just now"; }

// # Connection screens
function ConnectionScreen({ title, message, children }) {
  return <main className="connection-screen"><section className="connection-card"><img src="/assets/toolstead-logo.png" alt="Toolstead" /><span className="eyebrow">Toolstead workspace</span><h1>{title}</h1><p>{message}</p>{children}</section></main>;
}

function LoginScreen({ onAuthenticated }) {
  const [credentials, setCredentials] = useState({ email: "", password: "" });
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState("");
  const submit = async (event) => { event.preventDefault(); setSubmitting(true); setError(""); try { await onAuthenticated(await login(credentials)); } catch (reason) { setError(reason.message); } finally { setSubmitting(false); } };
  return <ConnectionScreen title="Sign in to your workspace" message="Use the owner account created during workspace setup."><form className="stack-form" onSubmit={submit}><label><span>Email address</span><input type="email" value={credentials.email} onChange={(e) => setCredentials({ ...credentials, email: e.target.value })} required /></label><label><span>Password</span><input type="password" minLength="12" value={credentials.password} onChange={(e) => setCredentials({ ...credentials, password: e.target.value })} required /></label>{error && <p className="form-error" role="alert">{error}</p>}<button className="primary-button" disabled={submitting}>{submitting ? <SpinnerGap className="spin" /> : <ShieldCheck />}{submitting ? "Signing in…" : "Sign in"}</button></form></ConnectionScreen>;
}

// # Accessible dialog
function Modal({ title, eyebrow, children, onClose, wide = false }) {
  const closeRef = useRef(null);
  useEffect(() => { closeRef.current?.focus(); const escape = (event) => event.key === "Escape" && onClose(); window.addEventListener("keydown", escape); return () => window.removeEventListener("keydown", escape); }, [onClose]);
  return <div className="modal-backdrop" onMouseDown={onClose}><section className={`modal ${wide ? "wide" : ""}`} role="dialog" aria-modal="true" aria-labelledby="modal-title" onMouseDown={(e) => e.stopPropagation()}><header className="modal-header"><div><span className="eyebrow">{eyebrow}</span><h2 id="modal-title">{title}</h2></div><button ref={closeRef} className="icon-button" onClick={onClose} aria-label="Close"><X weight="bold" /></button></header>{children}</section></div>;
}

// # Tool dashboard
function MyTools({ contacts, onNavigate, onCreateLead }) {
  const newLeads = contacts.filter((contact) => contact.stage === "New lead").length;
  return <main className="page-content"><header className="page-heading"><div><span className="eyebrow">Command center</span><h1>My Tools</h1><p>Everything enabled for this workspace, without the clutter.</p></div><button className="primary-button" onClick={onCreateLead}><Plus weight="bold" /> Add lead</button></header><section className="metric-grid" aria-label="Workspace snapshot"><article><span className="metric-icon blue"><Users /></span><div><small>Active contacts</small><strong>{contacts.length}</strong><span>Unified CRM records</span></div></article><article><span className="metric-icon orange"><Sparkle /></span><div><small>New leads</small><strong>{newLeads}</strong><span>Waiting for first response</span></div></article><article><span className="metric-icon teal"><CheckCircle /></span><div><small>Enabled tools</small><strong>1</strong><span>7 available to add later</span></div></article></section><section className="section-block"><div className="section-heading"><div><h2>Active tools</h2><p>Only subscribed tools appear in your workspace navigation.</p></div><button className="text-button" onClick={() => onNavigate("library")}>Browse Tool Library <ArrowRight /></button></div><article className="active-tool-card"><div className="tool-mark blue"><AddressBook weight="duotone" /></div><div className="tool-copy"><span className="status-pill active"><Check /> Active</span><h3>Lead Intake & CRM</h3><p>Capture leads, organize contact details, add notes, and see each customer interaction in one timeline.</p><div className="feature-row"><span><CheckCircle /> Lead capture</span><span><CheckCircle /> Contact directory</span><span><CheckCircle /> Unified timeline</span></div></div><button className="secondary-button" onClick={() => onNavigate("crm")}>Open CRM <ArrowRight /></button></article></section><section className="section-block next-up"><div><span className="eyebrow">Recommended next activation</span><h2>Turn inquiries into booked work</h2><p>Add Booking & Calendar when the CRM workflow is connected to production data.</p></div><span className="tool-mark teal"><CalendarBlank weight="duotone" /></span></section></main>;
}

// # Tool library
function ToolLibrary({ enabledKeys, onOpenTool }) {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All");
  const [selectedTool, setSelectedTool] = useState(null);
  const categories = ["All", ...new Set(TOOL_REGISTRY.map((tool) => tool.category))];
  const visible = TOOL_REGISTRY.filter((tool) => (category === "All" || tool.category === category) && (!query || `${tool.name} ${tool.description}`.toLowerCase().includes(query.toLowerCase())));
  return <main className="page-content"><header className="page-heading"><div><span className="eyebrow">Modular by design</span><h1>Tool Library</h1><p>Activate only what your business needs. Inactive tools stay out of your daily workspace.</p></div><span className="catalog-count">{TOOL_REGISTRY.length} tools</span></header><section className="library-toolbar"><label className="search-field"><MagnifyingGlass /><span className="sr-only">Search tools</span><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search tools and outcomes…" /></label><div className="category-tabs" aria-label="Tool categories">{categories.map((item) => <button key={item} className={category === item ? "active" : ""} onClick={() => setCategory(item)}>{item}</button>)}</div></section><section className="tool-grid">{visible.map((tool) => { const Icon = tool.icon; const active = enabledKeys.includes(tool.key); return <article className="tool-card" key={tool.key}><div className="tool-card-top"><span className={`tool-mark ${tool.accent}`}><Icon weight="duotone" /></span><span className={`status-pill ${active ? "active" : tool.status}`}>{active ? <Check /> : tool.status === "planned" ? <Wrench /> : <Plus />}{active ? "Active" : tool.status === "planned" ? "Planned" : "Available"}</span></div><span className="tool-category">{tool.category}</span><h2>{tool.name}</h2><p>{tool.description}</p><footer><small>{tool.included}</small>{active ? <button className="secondary-button" onClick={() => onOpenTool(tool.key)}>Open <ArrowRight /></button> : <button className="secondary-button" disabled={tool.status === "planned"} onClick={() => setSelectedTool(tool)}>{tool.status === "planned" ? "Roadmap" : "View access"}</button>}</footer></article>; })}</section>{!visible.length && <section className="empty-state"><MagnifyingGlass /><h2>No tools match</h2><p>Try another name or category.</p><button className="text-button" onClick={() => { setQuery(""); setCategory("All"); }}>Clear filters</button></section>}{selectedTool && <Modal title={selectedTool.name} eyebrow="Module access" onClose={() => setSelectedTool(null)}><div className="access-panel"><span className={`tool-mark ${selectedTool.accent}`}>{(() => { const Icon = selectedTool.icon; return <Icon weight="duotone" />; })()}</span><p>{selectedTool.description}</p><dl><div><dt>Access</dt><dd>{selectedTool.included}</dd></div><div><dt>Billing status</dt><dd>Not enforced during foundation build</dd></div></dl><p className="info-note">Activation is intentionally disabled until subscription plans and production billing are connected. No purchase will be created from this preview.</p><button className="primary-button" onClick={() => setSelectedTool(null)}>Got it</button></div></Modal>}</main>;
}

// # CRM workspace
function CrmWorkspace({ contacts, selectedId, setSelectedId, onCreate, onEdit, onArchive, onAddNote }) {
  const [query, setQuery] = useState("");
  const [stage, setStage] = useState("All");
  const [note, setNote] = useState("");
  const stages = ["All", "New lead", "Contacted", "Qualified", "Won"];
  const visible = contacts.filter((contact) => (stage === "All" || contact.stage === stage) && (!query || `${contact.displayName} ${contact.companyName} ${contact.email} ${contact.phone}`.toLowerCase().includes(query.toLowerCase())));
  const selected = contacts.find((contact) => contact.id === selectedId) || visible[0] || contacts[0];
  const submitNote = async (event) => { event.preventDefault(); if (!note.trim() || !selected) return; await onAddNote(selected.id, note.trim()); setNote(""); };
  return <main className="crm-page"><section className="crm-list-pane"><header className="crm-heading"><div><span className="eyebrow">Lead Intake & CRM</span><h1>Contacts</h1><p>{contacts.length} active records</p></div><button className="primary-button compact" onClick={onCreate}><Plus weight="bold" /> Add lead</button></header><label className="search-field contact-search"><MagnifyingGlass /><span className="sr-only">Search contacts</span><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search name, email, or phone…" /></label><div className="stage-filter"><Funnel /><select value={stage} onChange={(e) => setStage(e.target.value)} aria-label="Filter by stage">{stages.map((item) => <option key={item}>{item}</option>)}</select></div><div className="contact-list">{visible.map((contact) => <button key={contact.id} className={`contact-row ${selected?.id === contact.id ? "active" : ""}`} onClick={() => setSelectedId(contact.id)}><span className="avatar">{initials(contact.displayName)}</span><span className="contact-row-copy"><strong>{contact.displayName}</strong><small>{contact.companyName || contact.source}</small><span>{contact.email || contact.phone || "No contact channel"}</span></span><span className={`lead-stage ${contact.stage?.toLowerCase().replaceAll(" ", "-") || "new-lead"}`}>{contact.stage || "New lead"}</span></button>)}{!visible.length && <div className="empty-state compact"><Users /><strong>No contacts found</strong><span>Clear the search or add a new lead.</span></div>}</div></section><aside className="crm-detail-pane">{selected ? <><header className="contact-profile"><span className="avatar large">{initials(selected.displayName)}</span><div><span className={`lead-stage ${selected.stage?.toLowerCase().replaceAll(" ", "-") || "new-lead"}`}>{selected.stage || "New lead"}</span><h2>{selected.displayName}</h2><p>{selected.companyName || "Individual contact"}</p></div><button className="icon-button" onClick={() => onEdit(selected)} aria-label="Edit contact"><NotePencil /></button></header><section className="contact-methods"><a href={selected.email ? `mailto:${selected.email}` : undefined} className={!selected.email ? "disabled" : ""}><EnvelopeSimple /><span><small>Email</small><strong>{selected.email || "Not recorded"}</strong></span></a><a href={selected.phone ? `tel:${selected.phone}` : undefined} className={!selected.phone ? "disabled" : ""}><PhoneCall /><span><small>Phone</small><strong>{selected.phone || "Not recorded"}</strong></span></a></section><section className="timeline-section"><div className="section-heading"><div><h3>Activity timeline</h3><p>Every lead touchpoint in one place.</p></div></div><form className="note-composer" onSubmit={submitNote}><input value={note} onChange={(e) => setNote(e.target.value)} placeholder="Add an internal note…" aria-label="Internal note" /><button className="primary-button compact" disabled={!note.trim()}><PaperPlaneTilt /> Add note</button></form><div className="timeline">{(selected.timeline || []).map((event) => <article key={event.id}><span className="timeline-icon"><NotePencil /></span><div><time>{formatDate(event.occurredAt)}</time><h4>{event.title}</h4><p>{event.text}</p></div></article>)}{!selected.timeline?.length && <div className="empty-state compact"><Tray /><strong>No activity yet</strong><span>Add a note to start the timeline.</span></div>}</div></section><footer className="contact-footer"><button className="danger-text" onClick={() => onArchive(selected)}>Archive contact</button><span>Added from {selected.source}</span></footer></> : <div className="empty-state"><AddressBook /><h2>Select a contact</h2><p>Choose a record to view its timeline.</p></div>}</aside></main>;
}

// # Main application
export function App() {
  const [connectionState, setConnectionState] = useState("checking");
  const [workspaceName, setWorkspaceName] = useState("TNT Car Care");
  const [activePage, setActivePage] = useState("my-tools");
  const [contacts, setContacts] = useState(PREVIEW_CONTACTS);
  const [modules, setModules] = useState(TOOL_REGISTRY);
  const [selectedId, setSelectedId] = useState(PREVIEW_CONTACTS[0].id);
  const [modal, setModal] = useState(null);
  const [editingContact, setEditingContact] = useState(null);
  const [leadDraft, setLeadDraft] = useState(EMPTY_LEAD);
  const [saving, setSaving] = useState(false);
  const [formError, setFormError] = useState("");
  const [toast, setToast] = useState("");
  const [mobileNavOpen, setMobileNavOpen] = useState(false);
  const [accountOpen, setAccountOpen] = useState(false);

  const enabledKeys = useMemo(() => { const enabled = modules.filter((module) => module.enabled || module.status === "active").map((module) => module.key); return enabled.length ? enabled : ["crm-core"]; }, [modules]);
  const announce = (message) => { setToast(message); window.setTimeout(() => setToast(""), 2800); };
  const loadLiveData = async () => { const [modulePayload, contactPayload] = await Promise.all([getModules(), getContacts()]); setModules(modulePayload.modules); setContacts(contactPayload.contacts.map((contact) => ({ ...contact, stage: contact.stage || "New lead" }))); setSelectedId(contactPayload.contacts[0]?.id || ""); };

  useEffect(() => { let cancelled = false; (async () => { try { await checkApi(); await loadLiveData(); if (!cancelled) setConnectionState("api"); } catch (error) { if (cancelled) return; if (error instanceof ApiError && error.status === 401) setConnectionState("auth"); else setConnectionState("preview"); } })(); return () => { cancelled = true; }; }, []);

  const navigate = (page) => { setActivePage(page); setMobileNavOpen(false); window.scrollTo({ top: 0, behavior: "smooth" }); };
  const openCreate = () => { setEditingContact(null); setLeadDraft(EMPTY_LEAD); setFormError(""); setModal("lead"); };
  const openEdit = (contact) => { setEditingContact(contact); setLeadDraft({ displayName: contact.displayName, companyName: contact.companyName || "", email: contact.email || "", phone: contact.phone || "", source: contact.source || "Manual entry", summary: "" }); setFormError(""); setModal("lead"); };
  const saveLead = async (event) => { event.preventDefault(); setSaving(true); setFormError(""); try { if (connectionState === "api") { const payload = editingContact ? await updateContact(editingContact.id, leadDraft) : await createContact(leadDraft); await loadLiveData(); setSelectedId(payload.contact.id); } else if (editingContact) { setContacts((items) => items.map((item) => item.id === editingContact.id ? { ...item, ...leadDraft, updatedAt: new Date().toISOString() } : item)); } else { const created = { ...leadDraft, id: `preview-${Date.now()}`, stage: "New lead", updatedAt: new Date().toISOString(), timeline: [{ id: `event-${Date.now()}`, eventType: "lead_created", title: "Lead created", text: leadDraft.summary || `Added from ${leadDraft.source}.`, occurredAt: new Date().toISOString() }] }; setContacts((items) => [created, ...items]); setSelectedId(created.id); } setModal(null); setActivePage("crm"); announce(editingContact ? "Contact updated." : "Lead added."); } catch (error) { setFormError(error.message); } finally { setSaving(false); } };
  const addNote = async (contactId, body) => { if (connectionState === "api") { await addContactNote(contactId, body); await loadLiveData(); } else setContacts((items) => items.map((item) => item.id === contactId ? { ...item, updatedAt: new Date().toISOString(), timeline: [{ id: `preview-note-${Date.now()}`, eventType: "note_added", title: "Note added", text: body, occurredAt: new Date().toISOString() }, ...(item.timeline || [])] } : item)); announce("Note added to the timeline."); };
  const requestArchive = (contact) => { setEditingContact(contact); setModal("archive"); };
  const confirmArchive = async () => { if (connectionState === "api") await archiveContact(editingContact.id); setContacts((items) => items.filter((item) => item.id !== editingContact.id)); setSelectedId(contacts.find((item) => item.id !== editingContact.id)?.id || ""); setModal(null); announce("Contact archived."); };

  if (connectionState === "checking") return <ConnectionScreen title="Opening your workspace" message="Loading tools, entitlements, and customer records."><SpinnerGap className="spin connection-spinner" /></ConnectionScreen>;
  if (connectionState === "auth") return <LoginScreen onAuthenticated={async (account) => { setWorkspaceName(account.workspace.name); await loadLiveData(); setConnectionState("api"); }} />;

  return <div className="platform-shell"><header className="topbar"><button className="mobile-menu-button" onClick={() => setMobileNavOpen((open) => !open)} aria-label="Open navigation"><GridFour /></button><a className="brand" href="#my-tools" onClick={() => navigate("my-tools")}><img src="/assets/toolstead-logo.png" alt="Toolstead" /></a><button className="workspace-button"><span className="workspace-icon"><Storefront weight="fill" /></span><span><small>Workspace</small><strong>{workspaceName}</strong></span><CaretDown /></button><label className="global-search"><MagnifyingGlass /><span className="sr-only">Search workspace</span><input placeholder="Search workspace…" onFocus={() => activePage !== "crm" && navigate("crm")} /></label><div className="topbar-actions"><button className="icon-button dark" aria-label="Notifications"><Bell /></button><div className="account-wrap"><button className="profile-button" onClick={() => setAccountOpen((open) => !open)} aria-expanded={accountOpen}><img src="/assets/user-avatar.png" alt="Owner profile" /><CaretDown /></button>{accountOpen && <div className="popover account-menu"><strong>Owner account</strong><small>{workspaceName}</small><button onClick={() => { setAccountOpen(false); announce("Account settings are scheduled for the authentication phase."); }}>Account settings</button></div>}</div></div></header><aside className={`sidebar ${mobileNavOpen ? "open" : ""}`}><div className="sidebar-heading"><span>Workspace</span><button className="icon-button" onClick={() => setMobileNavOpen(false)} aria-label="Close navigation"><X /></button></div><nav aria-label="Primary navigation">{NAV_ITEMS.filter((item) => !item.toolKey || enabledKeys.includes(item.toolKey)).map(({ id, label, icon: Icon }) => <button key={id} className={activePage === id ? "active" : ""} onClick={() => navigate(id)} aria-current={activePage === id ? "page" : undefined}><Icon weight={activePage === id ? "fill" : "regular"} /><span>{label}</span>{id === "crm" && <span className="nav-count">{contacts.length}</span>}</button>)}</nav><div className="sidebar-tools"><span className="sidebar-label">Active tools</span><button className={activePage === "crm" ? "active" : ""} onClick={() => navigate("crm")}><AddressBook /><span>Lead Intake & CRM</span><CheckCircle weight="fill" /></button></div><button className="library-shortcut" onClick={() => navigate("library")}><Plus /><span><strong>Add another tool</strong><small>Browse the library</small></span></button><div className="plan-card"><span>Foundation plan</span><strong>1 of 8 tools active</strong><div><i /></div><button onClick={() => navigate("library")}>Manage tools</button></div></aside><section className="content-shell">{connectionState === "preview" && <div className="preview-banner"><span><Sparkle weight="fill" /></span><p><strong>Interactive preview mode</strong> — Sample records are clearly labeled and changes last only for this browser session.</p></div>}{activePage === "my-tools" && <MyTools contacts={contacts} onNavigate={navigate} onCreateLead={openCreate} />}{activePage === "library" && <ToolLibrary enabledKeys={enabledKeys} onOpenTool={(key) => key === "crm-core" && navigate("crm")} />}{activePage === "crm" && <CrmWorkspace contacts={contacts} selectedId={selectedId} setSelectedId={setSelectedId} onCreate={openCreate} onEdit={openEdit} onArchive={requestArchive} onAddNote={addNote} />}</section>{mobileNavOpen && <button className="mobile-scrim" onClick={() => setMobileNavOpen(false)} aria-label="Close navigation" />}{modal === "lead" && <Modal title={editingContact ? "Edit contact" : "Add a new lead"} eyebrow="Lead Intake & CRM" onClose={() => setModal(null)} wide><form className="lead-form" onSubmit={saveLead}><div className="form-grid"><label><span>Full name *</span><input value={leadDraft.displayName} onChange={(e) => setLeadDraft({ ...leadDraft, displayName: e.target.value })} required /></label><label><span>Company</span><input value={leadDraft.companyName} onChange={(e) => setLeadDraft({ ...leadDraft, companyName: e.target.value })} /></label><label><span>Email</span><input type="email" value={leadDraft.email} onChange={(e) => setLeadDraft({ ...leadDraft, email: e.target.value })} /></label><label><span>Phone</span><input type="tel" value={leadDraft.phone} onChange={(e) => setLeadDraft({ ...leadDraft, phone: e.target.value })} /></label><label><span>Lead source</span><select value={leadDraft.source} onChange={(e) => setLeadDraft({ ...leadDraft, source: e.target.value })}><option>Manual entry</option><option>Website form</option><option>Missed call</option><option>Referral</option><option>Booking page</option></select></label>{!editingContact && <label className="full"><span>Request summary</span><textarea rows="3" value={leadDraft.summary} onChange={(e) => setLeadDraft({ ...leadDraft, summary: e.target.value })} placeholder="What does this lead need?" /></label>}</div>{formError && <p className="form-error" role="alert">{formError}</p>}<footer className="modal-actions"><button type="button" className="text-button" onClick={() => setModal(null)}>Cancel</button><button className="primary-button" disabled={saving}><Check />{saving ? "Saving…" : editingContact ? "Save changes" : "Add lead"}</button></footer></form></Modal>}{modal === "archive" && <Modal title="Archive this contact?" eyebrow="Contact management" onClose={() => setModal(null)}><div className="confirm-panel"><p><strong>{editingContact?.displayName}</strong> will be removed from active CRM lists. Production-connected records remain recoverable in the database.</p><div className="modal-actions"><button className="text-button" onClick={() => setModal(null)}>Keep contact</button><button className="danger-button" onClick={confirmArchive}>Archive contact</button></div></div></Modal>}{toast && <div className="toast" role="status"><CheckCircle weight="fill" /> {toast}</div>}</div>;
}
