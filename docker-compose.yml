services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: toolstead
      POSTGRES_USER: toolstead
      POSTGRES_PASSWORD: toolstead
    ports:
      - "5432:5432"
    volumes:
      - toolstead_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U toolstead -d toolstead"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  toolstead_postgres:

